# summary

List identity resolution rulesets.

# description

Lists verified identity resolution rulesets.

# examples

- List rulesets:
  <%= config.bin %> <%= command.id %> --target-org my-org
- Return JSON:
  <%= config.bin %> <%= command.id %> --target-org my-org --json

# flags.all.summary

Fetch all rulesets.

# flags.limit.summary

Maximum rulesets to return.

# flags.result-format.summary

Output format.
