# summary

List segments.

# description

Lists verified Data 360 segments.

# examples

- List segments:
  <%= config.bin %> <%= command.id %> --target-org my-org
- Return JSON:
  <%= config.bin %> <%= command.id %> --target-org my-org --json

# flags.all.summary

Fetch all segments.

# flags.limit.summary

Maximum segments to return.

# flags.result-format.summary

Output format.
