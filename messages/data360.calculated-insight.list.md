# summary

List calculated insights.

# description

Lists verified calculated insight definitions.

# examples

- List calculated insights:
  <%= config.bin %> <%= command.id %> --target-org my-org
- Return JSON:
  <%= config.bin %> <%= command.id %> --target-org my-org --json

# flags.all.summary

Fetch all calculated insights.

# flags.limit.summary

Maximum calculated insights to return.

# flags.result-format.summary

Output format.
