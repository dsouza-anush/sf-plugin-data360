# error.D360_API_ERROR.0

The request could not use the configured enterprise proxy.

# error.D360_API_ERROR.0.actions.1

Check HTTP_PROXY and HTTPS_PROXY, without sharing any embedded credentials.

# error.D360_API_ERROR.0.actions.2

Add destinations that must connect directly to NO_PROXY, then try again.

# error.unsupportedProxyUrl

Unsupported proxy URL.
